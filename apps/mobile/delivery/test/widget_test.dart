import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App compiles smoke test', (WidgetTester tester) async {
    // Basic test to ensure test compilation passes
    expect(true, isTrue);
  });
}
